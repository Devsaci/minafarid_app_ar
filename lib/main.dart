import 'package:flutter/material.dart';

import 'app/app.dart';

void main() {
  runApp(MyApp());
  test().updateAppState();
  test().getAppState();
  test33().updateAppState();
  test33().getAppState();
}
